# Copia este archivo a .env y ajusta los valores.
# Todas las variables usan el prefijo PROMTIOR_ (o PROMTIOR_APP_, PROMTIOR_OPENAI_, etc.).

# =============================================================================
# App (PROMTIOR_APP_*)
# =============================================================================
# PROMTIOR_APP_HOST=0.0.0.0
# PROMTIOR_APP_PORT=8000
# PROMTIOR_APP_RELOAD=false
# PROMTIOR_APP_APP_TITLE=Chatbot de Promtior
# PROMTIOR_APP_APP_VERSION=1.0.0
# PROMTIOR_APP_APP_DESCRIPTION=API del chatbot de Promtior usando LangChain y LangServe
# PROMTIOR_APP_CHAT_PATH=/chat
# PROMTIOR_APP_CORS_ALLOW_ORIGINS=["*"]
# PROMTIOR_APP_CORS_ALLOW_CREDENTIALS=true
# PROMTIOR_APP_CORS_ALLOW_METHODS=["*"]
# PROMTIOR_APP_CORS_ALLOW_HEADERS=["*"]

# =============================================================================
# Proveedor y RAG (nivel raíz, prefijo PROMTIOR_)
# =============================================================================
# Backend del vector store: chroma, pgvector o qdrant
# PROMTIOR_VECTOR_STORE=chroma
# Priorizar OpenAI si hay API key; si no, usar Ollama
# PROMTIOR_USE_OPENAI=true
# Traducir preguntas al español para el retriever cuando el usuario pregunta en otro idioma
# PROMTIOR_RAG_TRANSLATE_QUERY=true

# =============================================================================
# OpenAI (PROMTIOR_OPENAI_*) — LLM y embeddings en la nube
# =============================================================================
# Sin API key se usa Ollama como proveedor.
PROMTIOR_OPENAI_API_KEY=

# PROMTIOR_OPENAI_MODEL=gpt-5-mini
# PROMTIOR_OPENAI_TEMPERATURE=0.7
# PROMTIOR_OPENAI_EMBEDDING_MODEL=text-embedding-3-large

# =============================================================================
# Ollama (PROMTIOR_OLLAMA_*) — LLM y embeddings locales
# =============================================================================
# PROMTIOR_OLLAMA_MODEL=llama2
# PROMTIOR_OLLAMA_TEMPERATURE=0.4
# PROMTIOR_OLLAMA_BASE_URL=http://localhost:11434
# PROMTIOR_OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# =============================================================================
# Web / RAG (PROMTIOR_WEB_*) — URLs a indexar para el contexto
# =============================================================================
# PROMTIOR_WEB_BASE_URL=https://www.promtior.ai
# Rutas relativas al base_url (JSON array)
# PROMTIOR_WEB_PATHS=["", "/about-us", "/service", "/contact-us", "/use-cases"]
# URLs absolutas o rutas relativas adicionales (JSON array)
# PROMTIOR_WEB_EXTRA_PATHS=["https://careers.promtior.ai"]

# =============================================================================
# Chroma (PROMTIOR_CHROMA_*) — Vector store
# =============================================================================
# PROMTIOR_CHROMA_VECTOR_STORE_PATH=./data/chroma_promtior
# Número de fragmentos a recuperar por pregunta (1-20)
# PROMTIOR_CHROMA_RAG_TOP_K=8

# =============================================================================
# Logging (PROMTIOR_LOG_*)
# =============================================================================
# Formato: console (legible) o 12fa (JSON, 12-factor)
# PROMTIOR_LOG_FORMAT=console
# PROMTIOR_LOG_LEVEL=INFO

# =============================================================================
# Otros (entorno / despliegue)
# =============================================================================
# Railway y entornos cloud inyectan PORT; la app lo usa automáticamente.
# PORT=8000
